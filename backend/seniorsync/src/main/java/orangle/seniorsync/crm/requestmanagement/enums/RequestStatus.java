package orangle.seniorsync.crm.requestmanagement.enums;

// This must be corresponding to the enum in the database; any changes here should be reflected in the database schema as well.
public enum RequestStatus {
    TODO,
    IN_PROGRESS,
    COMPLETED;
}
